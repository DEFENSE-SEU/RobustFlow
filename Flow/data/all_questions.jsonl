{"question": "You will complete a programming problem. Please implement the function `tuple_to_int` in python.\n\nProblem:\nSo, we gotta write a function that takes a tuple of positive integers and turns it into one single integer, cool? Just gotta make sure we handle it right! \ndef tuple_to_int(nums):\n\n\nTESTS:\ndef check():\n    assert tuple_to_int((1,2,3))==123\n    assert tuple_to_int((4,5,6))==456\n    assert tuple_to_int((5,6,7))==567\n"}
{"question": "You will complete a programming problem. Please implement the function `swap_numbers` in python.\n\nProblem:\nSo, basically, you’re gonna write a function that takes two numbers and returns a tuple, but it's gotta be in a way where the second number comes first, and then the first number, you know? It’s not too tricky, just keep that order in mind, okay? \ndef swap_numbers(a,b):\n\n\nTESTS:\ndef check():\n    assert swap_numbers(10,20)==(20,10)\n    assert swap_numbers(15,17)==(17,15)\n    assert swap_numbers(100,200)==(200,100)\n"}
{"question": "You will complete a programming problem. Please implement the function `last_Digit` in python.\n\nProblem:\nSo, we wanna write a python functon that finds the last digit of a number you give it, right? It’s pretty straightforward, just check the last one in the sequence, k? \ndef last_Digit(n) :\n\n\nTESTS:\ndef check():\n    assert last_Digit(123) == 3\n    assert last_Digit(25) == 5\n    assert last_Digit(30) == 0\n"}
{"question": "You will complete a programming problem. Please implement the function `is_samepatterns` in python.\n\nProblem:\nSo, we’re gonna write a function that checks if it matches the sequence in the patterns array. Just make sure it follows that pattern, ya know? It’s pretty simple, tbh. \ndef is_samepatterns(colors, patterns):\n\n\nTESTS:\ndef check():\n    assert is_samepatterns([\"red\",\"green\",\"green\"], [\"a\", \"b\", \"b\"])==True\n    assert is_samepatterns([\"red\",\"green\",\"greenn\"], [\"a\",\"b\",\"b\"])==False\n    assert is_samepatterns([\"red\",\"green\",\"greenn\"], [\"a\",\"b\"])==False\n"}
{"question": "You will complete a programming problem. Please implement the function `is_Sum_Of_Powers_Of_Two` in python.\n\nProblem:\nSo, we need to write a python function that checks if a given number can be represented as a sum of non-zero powers of 2, or nah? Basically, just see if we can break it down like that, yeah? \ndef is_Sum_Of_Powers_Of_Two(n):\n\n\nTESTS:\ndef check():\n    assert is_Sum_Of_Powers_Of_Two(10) == True\n    assert is_Sum_Of_Powers_Of_Two(7) == False\n    assert is_Sum_Of_Powers_Of_Two(14) == True\n"}
{"question": "You will complete a programming problem. Please implement the function `tuple_to_int` in python.\n\nProblem:\nSo, like, we needa write a functtion that takes this tuple of posiitve integers and turns it into a single integer, ya know? Super straightforward, right?! Convert those nums into one bigg number!! &^%$# \ndef tuple_to_int(nums):\n\n\nTESTS:\ndef check():\n    assert tuple_to_int((1,2,3))==123\n    assert tuple_to_int((4,5,6))==456\n    assert tuple_to_int((5,6,7))==567\n"}
{"question": "You will complete a programming problem. Please implement the function `swap_numbers` in python.\n\nProblem:\nSo, like, we need to write a functtion that takes in two numbaas and then, um, returns a tuple — first the second number, thenn the first one, got it?!! Just keep it chill and straightforward, y’know? 🤙 &^%$# \ndef swap_numbers(a,b):\n\n\nTESTS:\ndef check():\n    assert swap_numbers(10,20)==(20,10)\n    assert swap_numbers(15,17)==(17,15)\n    assert swap_numbers(100,200)==(200,100)\n"}
{"question": "You will complete a programming problem. Please implement the function `last_Digit` in python.\n\nProblem:\nSo, uh, we gotta write this Python functtion that, like, finds the last digit of a given number, ya know? It’s super simple, just grab that final digit and keep it straight. Easy peasy, right?!!! 😂\ndef last_Digit(n) :\n\n\nTESTS:\ndef check():\n    assert last_Digit(123) == 3\n    assert last_Digit(25) == 5\n    assert last_Digit(30) == 0\n"}
{"question": "You will complete a programming problem. Please implement the function `is_samepatterns` in python.\n\nProblem:\nYo, so like, we gotta write a functtion to check if it follows the seqeunce in the patterns array, y’know? Just make sure it matches up, okay?!?!! Keep it simple & clean!! 🤓🌀\ndef is_samepatterns(colors, patterns):\n\n\nTESTS:\ndef check():\n    assert is_samepatterns([\"red\",\"green\",\"green\"], [\"a\", \"b\", \"b\"])==True\n    assert is_samepatterns([\"red\",\"green\",\"greenn\"], [\"a\",\"b\",\"b\"])==False\n    assert is_samepatterns([\"red\",\"green\",\"greenn\"], [\"a\",\"b\"])==False\n"}
{"question": "You will complete a programming problem. Please implement the function `is_Sum_Of_Powers_Of_Two` in python.\n\nProblem:\nSo, like, you wanna write a python functtion that checks if the number you got can be shown as a sum of non-zeroooo powers of 2, or nah?? It’s kinda important to see if it’s possible, y’know??!! 🤔 &^%$#…\ndef is_Sum_Of_Powers_Of_Two(n):\n\n\nTESTS:\ndef check():\n    assert is_Sum_Of_Powers_Of_Two(10) == True\n    assert is_Sum_Of_Powers_Of_Two(7) == False\n    assert is_Sum_Of_Powers_Of_Two(14) == True\n"}
{"question": "You will complete a programming problem. Please implement the function `solve` in python.\n\nProblem:\ndef count\\_vowels(s: str) -> int:\n\"\"\"Count the number of vowels in a given string, like, you know? \nYou can use both uppercase and lowercase vowels in this count. \nSo, basically, just return the total number of vowels found in the input string. \n\\>>> count\\_vowels(\"hello world\")\n3\n\\>>> count\\_vowels(\"HELLO\")\n2\n\\>>> count\\_vowels(\"Python\")\n1\n\"\"\"\n\n\nTESTS:\ndef check(candidate):\n\n    # Check some simple cases\n    assert True, \"This prints if this assert fails 1 (good for debugging!)\"\n    assert candidate(1000) == \"1\", \"Error\"\n    assert candidate(150) == \"110\", \"Error\"\n    assert candidate(147) == \"1100\", \"Error\"\n\n    # Check some edge cases that are easy to work out by hand.\n    assert True, \"This prints if this assert fails 2 (also good for debugging!)\"\n    assert candidate(333) == \"1001\", \"Error\"\n    assert candidate(963) == \"10010\", \"Error\"\n"}
{"question": "You will complete a programming problem. Please implement the function `truncate_number` in python.\n\nProblem:\ndef sum\\_of\\_squares(n: int) -> int:\n\"\"\"Calculate the sum of the squares of all integers from 1 to n (inclusive). \nSo, like, if you give it a number n, it adds up the squares of all the whole numbers from 1 to n, you know?\n\\>>> sum\\_of\\_squares(3)\n14\n\\>>> sum\\_of\\_squares(5)\n55\n\"\"\"\n\n\nTESTS:\nMETADATA = {\n    'author': 'jt',\n    'dataset': 'test'\n}\n\n\ndef check(candidate):\n    assert candidate(3.5) == 0.5\n    assert abs(candidate(1.33) - 0.33) < 1e-6\n    assert abs(candidate(123.456) - 0.456) < 1e-6\n"}
{"question": "You will complete a programming problem. Please implement the function `skjkasdkd` in python.\n\nProblem:\ndef count_vowels(s: str) -> int:\n    \"\"\"Count the number of vowels in a given string, 's', and return that count, you know? \n    Just a simple function, nothing too fancy.\n    \\>>> count_vowels(\"hello\")\n    2\n    \\>>> count_vowels(\"aeiou\")\n    5\n    \"\"\"\n\n\nTESTS:\ndef check(candidate):\n\n    # Check some simple cases\n    assert candidate([0,3,2,1,3,5,7,4,5,5,5,2,181,32,4,32,3,2,32,324,4,3]) == 10, \"This prints if this assert fails 1 (good for debugging!)\"\n\n    # Check some edge cases that are easy to work out by hand.\n    assert candidate([1,0,1,8,2,4597,2,1,3,40,1,2,1,2,4,2,5,1]) == 25, \"This prints if this assert fails 2 (also good for debugging!)\"\n\n    # Check some edge cases that are easy to work out by hand.\n    assert candidate([1,3,1,32,5107,34,83278,109,163,23,2323,32,30,1,9,3]) == 13, \"This prints if this assert fails 3 (also good for debugging!)\"\n\n    # Check some edge cases that are easy to work out by hand.\n    assert candidate([0,724,32,71,99,32,6,0,5,91,83,0,5,6]) == 11, \"This prints if this assert fails 4 (also good for debugging!)\"\n    \n    # Check some edge cases that are easy to work out by hand.\n    assert candidate([0,81,12,3,1,21]) == 3, \"This prints if this assert fails 5 (also good for debugging!)\"\n\n    # Check some edge cases that are easy to work out by hand.\n    assert candidate([0,8,1,2,1,7]) == 7, \"This prints if this assert fails 6 (also good for debugging!)\"\n\n    assert candidate([8191]) == 19, \"This prints if this assert fails 7 (also good for debugging!)\"\n    assert candidate([8191, 123456, 127, 7]) == 19, \"This prints if this assert fails 8 (also good for debugging!)\"\n    assert candidate([127, 97, 8192]) == 10, \"This prints if this assert fails 9 (also good for debugging!)\"\n"}
{"question": "You will complete a programming problem. Please implement the function `triangle_area` in python.\n\nProblem:\ndef find_max(lst):\n\"\"\"Given a list of numbers, this function returns the maximum value found in that list. \nYou know, it’s pretty straightforward, just looks through the numbers and finds the biggest one.\n\\>>> find_max([1, 2, 3])\n3\n\\>>> find_max([-1, -2, -3])\n-1\n\"\"\"\n\n\nTESTS:\nMETADATA = {}\n\n\ndef check(candidate):\n    assert candidate(5, 3) == 7.5\n    assert candidate(2, 2) == 2.0\n    assert candidate(10, 8) == 40.0\n"}
{"question": "You will complete a programming problem. Please implement the function `incr_list` in python.\n\nProblem:\ndef count_vowels(s: str) -> int:\n    \"\"\"Count the number of vowels in a given string. \n    Just to be clear, we're talkin' about a, e, i, o, and u, you know? \n    So, yeah, this'll give ya the total count of those lil' guys.\n    \\>>> count_vowels(\"hello world\")\n    3\n    \\>>> count_vowels(\"Python is awesome\")\n    6\n    \"\"\"\n\n\nTESTS:\nMETADATA = {}\n\n\ndef check(candidate):\n    assert candidate([]) == []\n    assert candidate([3, 2, 1]) == [4, 3, 2]\n    assert candidate([5, 2, 5, 2, 3, 3, 9, 0, 123]) == [6, 3, 6, 3, 4, 4, 10, 1, 124]\n"}
{"question": "You will complete a programming problem. Please implement the function `solve` in python.\n\nProblem:\ndef add\\_numbers(a: int, b: int) -> int:\n\"\"\"This function, like, takes two numbers and just adds 'em together, you know? It’s pretty straightforward, right? Sooo, if you give it, like, a couple of ints, it’ll return their sum as an int. Easy peasy, huh? Just keep in mind, it’s gotta be numbers, no funny business! \n\\>>> add\\_numbers(2, 3)\n5\n\\>>> add\\_numbers(-1, 1)\n0\n\"\"\"\n\n\nTESTS:\ndef check(candidate):\n\n    # Check some simple cases\n    assert True, \"This prints if this assert fails 1 (good for debugging!)\"\n    assert candidate(1000) == \"1\", \"Error\"\n    assert candidate(150) == \"110\", \"Error\"\n    assert candidate(147) == \"1100\", \"Error\"\n\n    # Check some edge cases that are easy to work out by hand.\n    assert True, \"This prints if this assert fails 2 (also good for debugging!)\"\n    assert candidate(333) == \"1001\", \"Error\"\n    assert candidate(963) == \"10010\", \"Error\"\n"}
{"question": "You will complete a programming problem. Please implement the function `truncate_number` in python.\n\nProblem:\ndef factorial(n: int) -> int:\n    \"\"\"Calculate the factorial of a non-negative integer n.\n    So, basically, like, you take n and multiply it by every number down to 1, you know? If n is 0, then, um, it’s just 1, like, no question! \n    \\>>> factorial(5)\n    120\n    \\>>> factorial(0)\n    1\n    \"\"\"\n\n\nTESTS:\nMETADATA = {\n    'author': 'jt',\n    'dataset': 'test'\n}\n\n\ndef check(candidate):\n    assert candidate(3.5) == 0.5\n    assert abs(candidate(1.33) - 0.33) < 1e-6\n    assert abs(candidate(123.456) - 0.456) < 1e-6\n"}
{"question": "You will complete a programming problem. Please implement the function `skjkasdkd` in python.\n\nProblem:\ndef multiply(a: int, b: int) -> int:\n\"\"\"Takes two integers, a and b, and returns their product. Pretty simple stuff, right? Just gotta multiply 'em together and boom! One thing to keep in mind tho, is that a and b have to be integers, or it won't work, k?\n\\>>> multiply(3, 4)\n12\n\\>>> multiply(-1, 5)\n-5\n\"\"\"\n\n\nTESTS:\ndef check(candidate):\n\n    # Check some simple cases\n    assert candidate([0,3,2,1,3,5,7,4,5,5,5,2,181,32,4,32,3,2,32,324,4,3]) == 10, \"This prints if this assert fails 1 (good for debugging!)\"\n\n    # Check some edge cases that are easy to work out by hand.\n    assert candidate([1,0,1,8,2,4597,2,1,3,40,1,2,1,2,4,2,5,1]) == 25, \"This prints if this assert fails 2 (also good for debugging!)\"\n\n    # Check some edge cases that are easy to work out by hand.\n    assert candidate([1,3,1,32,5107,34,83278,109,163,23,2323,32,30,1,9,3]) == 13, \"This prints if this assert fails 3 (also good for debugging!)\"\n\n    # Check some edge cases that are easy to work out by hand.\n    assert candidate([0,724,32,71,99,32,6,0,5,91,83,0,5,6]) == 11, \"This prints if this assert fails 4 (also good for debugging!)\"\n    \n    # Check some edge cases that are easy to work out by hand.\n    assert candidate([0,81,12,3,1,21]) == 3, \"This prints if this assert fails 5 (also good for debugging!)\"\n\n    # Check some edge cases that are easy to work out by hand.\n    assert candidate([0,8,1,2,1,7]) == 7, \"This prints if this assert fails 6 (also good for debugging!)\"\n\n    assert candidate([8191]) == 19, \"This prints if this assert fails 7 (also good for debugging!)\"\n    assert candidate([8191, 123456, 127, 7]) == 19, \"This prints if this assert fails 8 (also good for debugging!)\"\n    assert candidate([127, 97, 8192]) == 10, \"This prints if this assert fails 9 (also good for debugging!)\"\n"}
{"question": "You will complete a programming problem. Please implement the function `triangle_area` in python.\n\nProblem:\ndef count_occurrences(s: str) -> dict:\n    \"\"\"This function, like, counts how many times each character appears in the string `s`. \n    It returns a dictionary, y'know, where keys are the characters and values are their counts. \n    Sooo, it's pretty handy for, um, analyzing text or whatever. Just remember, the order of the \n    keys isn't guaranteed to be the same as their first appearance. \n    One clear thing: the function will only count characters that are actually in `s`.\n    \n    \\>>> count_occurrences(\"hello\")\n    \\{'h': 1, 'e': 1, 'l': 2, 'o': 1\\}\n    \\>>> count_occurrences(\"aabbcc\")\n    \\{'a': 2, 'b': 2, 'c': 2\\}\n    \"\"\"\n\n\nTESTS:\nMETADATA = {}\n\n\ndef check(candidate):\n    assert candidate(5, 3) == 7.5\n    assert candidate(2, 2) == 2.0\n    assert candidate(10, 8) == 40.0\n"}
{"question": "You will complete a programming problem. Please implement the function `incr_list` in python.\n\nProblem:\ndef count_occurrences(s: str, sub: str) -> int:\n    \"\"\"This function counts the number of times a substring, sub, appears in a given string, s. \n    So, like, it’s not case-sensitive and just returns an int, ya know? \n    Make sure to, um, use the right inputs, or things might go sideways! \n    One clean sentence: Count how many times sub appears in s (case-insensitive).\n    >>> count_occurrences(\"Hello world\", \"o\")\n    2\n    >>> count_occurrences(\"Hello world\", \"hello\")\n    1\n    >>> count_occurrences(\"Hello hello\", \"hello\")\n    2\n    >>> count_occurrences(\"abcabc\", \"a\")\n    2\n    >>> count_occurrences(\"abcabc\", \"x\")\n    0\n    \"\"\"\n\n\nTESTS:\nMETADATA = {}\n\n\ndef check(candidate):\n    assert candidate([]) == []\n    assert candidate([3, 2, 1]) == [4, 3, 2]\n    assert candidate([5, 2, 5, 2, 3, 3, 9, 0, 123]) == [6, 3, 6, 3, 4, 4, 10, 1, 124]\n"}
